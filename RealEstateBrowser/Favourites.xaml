<Page
    x:Class="RealEstateBrowser.Favourites"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RealEstateBrowser"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:data="using:RealEstateBrowser.Models"
    mc:Ignorable="d">

    <Page.Transitions>
        <TransitionCollection>
            <NavigationThemeTransition>
                <NavigationThemeTransition.DefaultNavigationTransitionInfo>
                    <SlideNavigationTransitionInfo/>
                </NavigationThemeTransition.DefaultNavigationTransitionInfo>
            </NavigationThemeTransition>
        </TransitionCollection>
    </Page.Transitions>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="18*" />
        </Grid.RowDefinitions>

        <GridView x:Name="favsGrid" Grid.Row="1" ItemsSource="{x:Bind houses}" IsItemClickEnabled="True" ItemClick="favsGrid_ItemClick">
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="data:House">
                    <StackPanel>
                        <Image Width="250" Source="{x:Bind iconImg}" Stretch="UniformToFill" />
                        <TextBlock Text="{x:Bind beds}" />
                        <TextBlock Text="{x:Bind baths}" />
                    </StackPanel>
                </DataTemplate>                
            </GridView.ItemTemplate>
        </GridView>

        <TextBlock 
            x:Name="faveStatus"
            Text=""
            Visibility="Collapsed"
            Grid.Row="1" 
        />

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="10*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button
                Grid.Column="2"
                FontFamily="Segoe MDL2 Assets"
                FontSize="22"
                Content="&#xE8BB;"
                VerticalAlignment="Top"
                HorizontalAlignment="Right"
                Margin="0 10 5 0"
                Foreground="#000000"
                Background="Transparent"
                x:Name="closeFavourites"
                Click="closeFavourites_Click"
            />
        </Grid>

    </Grid>
</Page>
